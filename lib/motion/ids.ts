export function postTitleId(slug: string) {
  return `post-title-${slug}`;
}

export function postCoverId(slug: string) {
  return `post-cover-${slug}`;
}

export function postMetaId(slug: string) {
  return `post-meta-${slug}`;
}
